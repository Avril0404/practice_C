#include <stdio.h>

int main(){
    int a, b, c;

    a = 10;  //plug 10 into a
    b = 15;  //plug 15 into b

    c = a + b; //sub a+b into c

    printf("%d + %d = %d", a, b, c);

    return 0;
}